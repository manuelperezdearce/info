import random

# a = random.randrange(start=1,stop=10)
# b = random.randrange(start=1,stop=10)
# c = random.randrange(start=1,stop=10)

# resultado = -b + (b ** 2) - (a * c) * (4 * a + b) - (a ** 2)

# print(resultado)


# Calcular radio de círculo

radio = random.randrange(1,10) # Variable
PI = 3.1416                    # Constante (por convención)
PERIMETRO = 2*PI*radio         # Constante (por convención)

print(PERIMETRO)